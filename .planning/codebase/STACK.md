# Technology Stack

**Analysis Date:** 2026-01-21

## Languages

**Primary:**
- TypeScript 5.8.3 - All application code
- JavaScript (ES2017) - Configuration files

**Secondary:**
- CSS - Via Tailwind CSS utility classes
- HTML - Generated by React/Next.js components

## Runtime

**Environment:**
- Node.js - Web server and build-time execution
- Browser - Client-side rendering and interactions

**Package Manager:**
- pnpm - Lockfile: `pnpm-lock.yaml` (v9.0)

## Frameworks

**Core:**
- Next.js 15.3.8 - Web framework with App Router
- React 19.1.4 - UI rendering library
- React DOM 19.1.4 - React web rendering

**Styling:**
- Tailwind CSS 4.1.10 - Utility-first CSS framework
- PostCSS 4.1.18 - CSS transformation (`postcss.config.mjs`)

**Animations & Interactions:**
- Framer Motion 12.23.26 - Component animations and transitions
- Split-Type 0.3.4 - Text animation and splitting
- Lenis 1.3.16 - Smooth scroll library

**UI Components:**
- Lucide React 0.561.0 - Icon library

**Forms:**
- @formspree/react 3.0.0 - Form handling and email delivery

## Key Dependencies

**Critical:**
- @sentry/nextjs 10.31.0 - Error tracking and performance monitoring
- @next/third-parties 15.3.3 - Optimized integrations (Google Analytics)

**Infrastructure:**
- import-in-the-middle 2.0.0 - AST-based import middleware (Sentry instrumentation)
- require-in-the-middle 8.0.1 - Module require interception (Sentry instrumentation)

## Build & Development Tools

**Development:**
- ESLint 9.29.0 - Code linting with Next.js config
- eslint-config-next 15.3.3 - Next.js-specific ESLint rules
- eslint-plugin-react-hooks 5.2.0 - React Hooks linting
- @eslint/eslintrc 3.3.3 - ESLint configuration utilities

**Code Formatting:**
- Prettier 3.5.3 - Code formatting (`pnpm format` command)

**Type Definitions:**
- @types/node 24.0.2 - Node.js types
- @types/react 19.1.8 - React types
- @types/react-dom 19.1.6 - React DOM types

## Configuration

**TypeScript:**
- `tsconfig.json`:
  - Target: ES2017
  - Module: esnext
  - JSX: preserve (handled by Next.js)
  - Strict mode: enabled
  - Path alias: `@/*` → `./src/*`
  - Next.js plugin configured for type support

**Next.js:**
- `next.config.ts`:
  - Sentry integration via `withSentryConfig` wrapper
  - Project: `zml-cv`
  - Organization: `lagden-development`
  - Tunnel route: `/monitoring` (for ad-blocker circumvention)
  - Automatic React component annotation enabled
  - Vercel Cron Monitors integration enabled

**CSS/Styling:**
- `postcss.config.mjs` - PostCSS plugin: `@tailwindcss/postcss`

**Code Quality:**
- ESLint configuration via `eslintrc` (Next.js default)
- Prettier formatting via package.json scripts

## Environment Configuration

**Development:**
```bash
pnpm dev              # Development server with hot reload
pnpm devturbo         # Development with Turbopack for faster refresh
```

**Production:**
```bash
pnpm build            # Production build with optimizations
pnpm start            # Serve production build
```

**Code Quality:**
```bash
pnpm lint             # Run ESLint
pnpm format           # Format with Prettier
pnpm format:check     # Check formatting without changes
```

## Platform Requirements

**Development:**
- Node.js (current LTS or later)
- pnpm package manager
- Git for version control

**Production:**
- Node.js runtime for server-side rendering
- Deployment to Vercel or Node.js-compatible hosting (currently deployed to Vercel based on Sentry Cron configuration)

## External Services Integration

**Error Tracking:**
- Sentry (DSN in `sentry.server.config.ts` and `sentry.edge.config.ts`)
- Automatic instrumentation in `src/instrumentation.ts`
- Tunnel route configured at `/monitoring`

**Analytics:**
- Google Analytics (ID from `public/content.json` metadata)
- Integrated via `@next/third-parties/google`

---

*Stack analysis: 2026-01-21*
