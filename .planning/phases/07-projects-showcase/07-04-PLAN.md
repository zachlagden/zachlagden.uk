---
phase: 07-projects-showcase
plan: 04
type: execute
wave: 3
depends_on: [07-02, 07-03]
files_modified:
  - src/app/projects/page.tsx
  - src/app/projects/loading.tsx
autonomous: false

must_haves:
  truths:
    - "User can view list of projects at /projects"
    - "User can filter projects by clicking technology badges"
    - "Projects display title, description, technologies, and links"
    - "Projects with GitHub repos show stars and forks"
    - "Filters persist in URL and are shareable"
  artifacts:
    - path: "src/app/projects/page.tsx"
      provides: "Projects listing page with filtering"
      min_lines: 60
    - path: "src/app/projects/loading.tsx"
      provides: "Loading skeleton for projects page"
      min_lines: 20
  key_links:
    - from: "src/app/projects/page.tsx"
      to: "src/lib/projects/projects.ts"
      via: "DAL function calls"
      pattern: "getProjects|getAllTechnologies"
    - from: "src/app/projects/page.tsx"
      to: "src/lib/projects/github.ts"
      via: "GitHub stats fetching"
      pattern: "getProjectGitHubStats"
    - from: "src/app/projects/page.tsx"
      to: "src/components/projects/"
      via: "component imports"
      pattern: "import.*ProjectCard|TechnologyFilters|EmptyState"
---

<objective>
Create the /projects page integrating all components and verify the complete feature.

Purpose: Deliver the fully functional projects showcase with filtering, GitHub stats, and proper UX following the established blog page patterns.

Output: Working /projects route that satisfies all Phase 7 success criteria.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-projects-showcase/07-RESEARCH.md
@.planning/phases/07-projects-showcase/07-01-SUMMARY.md
@.planning/phases/07-projects-showcase/07-02-SUMMARY.md
@.planning/phases/07-projects-showcase/07-03-SUMMARY.md

# Page pattern to follow
@src/app/blog/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Projects Page</name>
  <files>src/app/projects/page.tsx</files>
  <action>
Create the projects listing page following the blog page pattern:

```tsx
import { Suspense } from "react";
import type { Metadata } from "next";
import { getProjects, getAllTechnologies } from "@/lib/projects/projects";
import { getProjectGitHubStats, GitHubStats } from "@/lib/projects/github";
import { ProjectCard } from "@/components/projects/ProjectCard";
import { TechnologyFilters } from "@/components/projects/TechnologyFilters";
import { EmptyState } from "@/components/projects/EmptyState";

// Force dynamic rendering - projects come from database
export const dynamic = "force-dynamic";

export const metadata: Metadata = {
  title: "Projects",
  description:
    "Explore my portfolio of projects, from web applications to open-source contributions. Filter by technology to find what interests you.",
};

interface ProjectsPageProps {
  searchParams: Promise<{
    tech?: string;
  }>;
}

export default async function ProjectsPage({ searchParams }: ProjectsPageProps) {
  // Await searchParams (Next.js 15 Promise-based API)
  const params = await searchParams;

  // Parse technology filter from URL
  const technologies = params.tech?.split(",").filter(Boolean) || [];

  // Fetch filtered projects and all technologies for filter UI
  const [projects, allTechnologies] = await Promise.all([
    getProjects({ technologies: technologies.length > 0 ? technologies : undefined }),
    getAllTechnologies(),
  ]);

  // Fetch GitHub stats for all projects with githubRepo (parallel)
  const statsPromises = projects.map((project) =>
    project.githubRepo ? getProjectGitHubStats(project.githubRepo) : Promise.resolve(null)
  );
  const allStats = await Promise.all(statsPromises);

  // Create a map of project ID to stats
  const statsMap = new Map<string, GitHubStats | null>();
  projects.forEach((project, index) => {
    statsMap.set(project._id, allStats[index]);
  });

  return (
    <div className="min-h-screen bg-white dark:bg-neutral-950">
      {/* Header */}
      <div className="border-b border-neutral-200 dark:border-neutral-800">
        <div className="container mx-auto px-4 sm:px-6 lg:px-8 py-12">
          <h1 className="text-4xl font-bold mb-4 text-neutral-900 dark:text-neutral-100">
            Projects
          </h1>
          <p className="text-lg text-neutral-600 dark:text-neutral-400 max-w-2xl">
            A collection of projects I&apos;ve built, from full-stack applications to
            open-source contributions. Filter by technology to explore.
          </p>
        </div>
      </div>

      {/* Main Content */}
      <div className="container mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Filters */}
        {allTechnologies.length > 0 && (
          <div className="mb-8">
            <h2 className="text-sm font-medium text-neutral-700 dark:text-neutral-300 mb-3">
              Filter by technology
            </h2>
            <Suspense
              fallback={
                <div className="h-10 bg-neutral-100 dark:bg-neutral-800 rounded-lg animate-pulse" />
              }
            >
              <TechnologyFilters technologies={allTechnologies} />
            </Suspense>
          </div>
        )}

        {/* Projects Grid */}
        {projects.length > 0 ? (
          <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
            {projects.map((project) => (
              <ProjectCard
                key={project._id}
                project={project}
                stats={statsMap.get(project._id)}
              />
            ))}
          </div>
        ) : (
          <EmptyState technologies={technologies} />
        )}
      </div>
    </div>
  );
}
```

Key implementation details:
1. **Dynamic rendering**: Database-driven content requires force-dynamic
2. **Parallel data fetching**: Projects and technologies fetched together
3. **Parallel GitHub stats**: All project stats fetched in parallel after projects load
4. **Stats map**: Efficient lookup of stats by project ID for rendering
5. **Suspense boundary**: Around TechnologyFilters for useQueryState compatibility
6. **Responsive grid**: 1 column mobile, 2 tablet, 3 desktop
7. **SEO metadata**: Title and description for the page
  </action>
  <verify>
1. `npx tsc --noEmit src/app/projects/page.tsx` - No TypeScript errors
2. Page follows blog/page.tsx patterns
  </verify>
  <done>Projects page renders list with filtering and GitHub stats integration</done>
</task>

<task type="auto">
  <name>Task 2: Create Loading Skeleton</name>
  <files>src/app/projects/loading.tsx</files>
  <action>
Create a loading skeleton that matches the page structure:

```tsx
export default function ProjectsLoading() {
  return (
    <div className="min-h-screen bg-white dark:bg-neutral-950">
      {/* Header skeleton */}
      <div className="border-b border-neutral-200 dark:border-neutral-800">
        <div className="container mx-auto px-4 sm:px-6 lg:px-8 py-12">
          <div className="h-10 w-48 bg-neutral-200 dark:bg-neutral-800 rounded animate-pulse mb-4" />
          <div className="h-6 w-96 max-w-full bg-neutral-200 dark:bg-neutral-800 rounded animate-pulse" />
        </div>
      </div>

      {/* Content skeleton */}
      <div className="container mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Filter skeleton */}
        <div className="mb-8">
          <div className="h-4 w-32 bg-neutral-200 dark:bg-neutral-800 rounded animate-pulse mb-3" />
          <div className="flex flex-wrap gap-2">
            {[1, 2, 3, 4, 5].map((i) => (
              <div
                key={i}
                className="h-8 w-20 bg-neutral-200 dark:bg-neutral-800 rounded-full animate-pulse"
              />
            ))}
          </div>
        </div>

        {/* Project cards skeleton */}
        <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
          {[1, 2, 3, 4, 5, 6].map((i) => (
            <div
              key={i}
              className="border border-neutral-200 dark:border-neutral-700 rounded-lg overflow-hidden"
            >
              {/* Image skeleton */}
              <div className="aspect-[16/9] bg-neutral-200 dark:bg-neutral-800 animate-pulse" />
              {/* Content skeleton */}
              <div className="p-6 space-y-4">
                <div className="h-6 w-3/4 bg-neutral-200 dark:bg-neutral-800 rounded animate-pulse" />
                <div className="space-y-2">
                  <div className="h-4 w-full bg-neutral-200 dark:bg-neutral-800 rounded animate-pulse" />
                  <div className="h-4 w-5/6 bg-neutral-200 dark:bg-neutral-800 rounded animate-pulse" />
                </div>
                <div className="flex gap-2">
                  {[1, 2, 3].map((j) => (
                    <div
                      key={j}
                      className="h-5 w-16 bg-neutral-200 dark:bg-neutral-800 rounded-full animate-pulse"
                    />
                  ))}
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}
```

The skeleton:
- Matches page structure exactly
- Shows 6 placeholder cards (grid fills nicely)
- Animates with pulse effect
- Supports both light and dark modes
  </action>
  <verify>`npx tsc --noEmit src/app/projects/loading.tsx` - No TypeScript errors</verify>
  <done>Loading skeleton provides visual feedback during page load</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Projects Showcase feature:
- /projects page with project listing
- Technology-based filtering via URL params
- GitHub stats display (stars, forks) for repos
- Responsive grid layout with cards
- Loading skeleton during navigation
  </what-built>
  <how-to-verify>
**Setup** (if no projects exist yet):
1. Add sample projects to MongoDB `projects` collection with this structure:
```json
{
  "slug": "sample-project",
  "title": "Sample Project",
  "description": "A sample project for testing the projects showcase.",
  "technologies": ["React", "Next.js", "TypeScript"],
  "demoUrl": "https://example.com",
  "sourceUrl": "https://github.com/zachlagden/sample",
  "githubRepo": "zachlagden/zachlagden.uk",
  "featuredImage": "/images/blog/default-cover.jpg",
  "published": true,
  "featured": false,
  "createdAt": { "$date": "2024-01-01T00:00:00Z" },
  "updatedAt": { "$date": "2024-01-01T00:00:00Z" }
}
```

**Verification steps:**
1. Start dev server: `pnpm dev`
2. Visit http://localhost:3000/projects
3. Verify page loads with header and description
4. Verify projects display with:
   - Title and description
   - Technology badges with colors
   - Demo/Source links (if set)
   - GitHub stars/forks (if githubRepo is set and public)
   - Featured badge on featured projects
5. Click a technology badge to filter
6. Verify URL updates (e.g., /projects?tech=React)
7. Verify only matching projects show
8. Click "Clear all filters" to reset
9. Test dark mode toggle - all elements visible
10. Test responsive layout on mobile viewport
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm dev` - Dev server starts without errors
2. `/projects` page loads and displays projects
3. Technology filters update URL and filter results
4. GitHub stats display for repos with githubRepo field
5. Empty state shows when filters match no projects
6. Loading skeleton appears during navigation
7. Page works in both light and dark modes
</verification>

<success_criteria>
From Phase 7 requirements:
- [x] User can view a list of projects at /projects
- [x] Each project displays title, description, technology stack as badges, and links
- [x] User can filter projects by clicking a technology badge
- [x] Projects with GitHub repos can optionally display live stats (stars, commits)
</success_criteria>

<output>
After completion, create `.planning/phases/07-projects-showcase/07-04-SUMMARY.md`
</output>
