---
phase: 01-dark-mode
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/ui/Section.tsx
  - src/components/ui/AboutCard.tsx
  - src/components/ui/GlobalBackground.tsx
  - src/components/ui/ScrollProgress.tsx
  - src/components/ui/ScrollToTopButton.tsx
  - src/components/ui/NavItem.tsx
  - src/components/ui/SkillCategory.tsx
  - src/components/ui/CertificationItem.tsx
  - src/components/ui/TimelineItem.tsx
  - src/components/ui/KeyboardIndicator.tsx
  - src/components/ui/CopyButton.tsx
  - src/components/ui/SocialIcon.tsx
  - src/components/layout/Header.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/Navigation.tsx
  - src/components/layout/MobileMenu.tsx
  - src/components/sections/AboutSection.tsx
  - src/components/sections/ExperienceSection.tsx
  - src/components/sections/EducationSection.tsx
  - src/components/sections/SkillsSection.tsx
  - src/components/sections/CertificationsSection.tsx
  - src/components/sections/ContactSection.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All existing UI elements display correctly in both themes"
    - "Text is readable in both light and dark modes"
    - "Cards and sections have appropriate contrast and depth"
    - "Interactive elements have visible hover/focus states in both themes"
  artifacts:
    - path: "src/components/ui/Section.tsx"
      provides: "Theme-aware section wrapper"
      contains: "dark:"
    - path: "src/components/layout/Header.tsx"
      provides: "Theme-aware header"
      contains: "dark:"
    - path: "src/components/ui/AboutCard.tsx"
      provides: "Theme-aware card component"
      contains: "dark:"
  key_links:
    - from: "all components"
      to: "Tailwind dark: prefix"
      via: "className with dark: variants"
      pattern: "dark:"
---

<objective>
Update all existing UI components with dark mode styles using Tailwind's dark: prefix.

Purpose: Ensures every visible element on the site displays correctly in dark mode. This is the comprehensive styling pass that makes the dark theme feel polished and intentional, not just inverted colors.

Output: All components respond to theme changes with appropriate colors, contrasts, and effects.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-dark-mode/01-CONTEXT.md
@.planning/phases/01-dark-mode/01-RESEARCH.md
@.planning/phases/01-dark-mode/01-01-SUMMARY.md

# Key color decisions from CONTEXT.md:
# - Dark background: #0a0a0a (not pure black)
# - Dark text: #e5e5e5 (off-white)
# - Elevated surfaces: #1a1a1a
# - Shadows -> subtle glow in dark mode
# - Images: 10-15% brightness reduction
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update layout components (Header, Footer, Navigation, MobileMenu)</name>
  <files>
    src/components/layout/Header.tsx
    src/components/layout/Footer.tsx
    src/components/layout/Navigation.tsx
    src/components/layout/MobileMenu.tsx
  </files>
  <action>
Update all layout components with dark mode variants.

**Color mapping pattern (apply throughout):**
| Light Mode | Dark Mode | Tailwind Classes |
|------------|-----------|------------------|
| bg-white | bg-[#0a0a0a] | `bg-white dark:bg-[#0a0a0a]` |
| bg-neutral-50 | bg-[#0a0a0a] | `bg-neutral-50 dark:bg-[#0a0a0a]` |
| bg-gray-50 | bg-[#1a1a1a] | `bg-gray-50 dark:bg-[#1a1a1a]` |
| text-neutral-800 | text-[#e5e5e5] | `text-neutral-800 dark:text-[#e5e5e5]` |
| text-neutral-600 | text-neutral-400 | `text-neutral-600 dark:text-neutral-400` |
| text-neutral-500 | text-neutral-400 | `text-neutral-500 dark:text-neutral-400` |
| border-neutral-200 | border-neutral-700 | `border-neutral-200 dark:border-neutral-700` |
| hover:text-neutral-800 | hover:text-white | `hover:text-neutral-800 dark:hover:text-white` |

**Header.tsx specific updates:**
1. Background gradient: `from-neutral-900/5` -> `from-white/5` in dark mode
2. Text colors: neutral-800 -> [#e5e5e5], neutral-600 -> neutral-400
3. Link hovers: hover:text-neutral-800 -> dark:hover:text-white
4. Scroll indicator gradient: from-neutral-300 -> dark:from-neutral-600
5. Focus rings: ring-black -> dark:ring-white

**Navigation.tsx specific updates:**
1. Background: likely uses neutral-50 or white -> dark variants
2. Active states: ensure visible in both modes
3. Hover states: appropriate contrast

**MobileMenu.tsx specific updates:**
1. Overlay background: adjust opacity for dark mode if needed
2. Menu background: white -> #0a0a0a
3. Text and borders: apply mapping

**Footer.tsx specific updates:**
1. Background: neutral-50 -> #0a0a0a
2. Text: neutral-600/800 -> #e5e5e5/neutral-400
3. Border: neutral-200 -> neutral-700
4. Links: appropriate hover states
  </action>
  <verify>
1. Run `grep -c "dark:" src/components/layout/Header.tsx` - should find dark: classes
2. Run `grep -c "dark:" src/components/layout/Footer.tsx` - should find dark: classes
3. Run `grep -c "dark:" src/components/layout/Navigation.tsx` - should find dark: classes
4. Run `grep -c "dark:" src/components/layout/MobileMenu.tsx` - should find dark: classes
5. Run `pnpm dev` and toggle theme - header/footer should update colors
6. Run `pnpm build` - should complete without errors
  </verify>
  <done>
All layout components have dark mode styles, visible and readable in both themes
  </done>
</task>

<task type="auto">
  <name>Task 2: Update UI components (Section, cards, buttons, indicators)</name>
  <files>
    src/components/ui/Section.tsx
    src/components/ui/AboutCard.tsx
    src/components/ui/GlobalBackground.tsx
    src/components/ui/ScrollProgress.tsx
    src/components/ui/ScrollToTopButton.tsx
    src/components/ui/NavItem.tsx
    src/components/ui/SkillCategory.tsx
    src/components/ui/CertificationItem.tsx
    src/components/ui/TimelineItem.tsx
    src/components/ui/KeyboardIndicator.tsx
    src/components/ui/CopyButton.tsx
    src/components/ui/SocialIcon.tsx
  </files>
  <action>
Update all UI components with dark mode variants.

**Section.tsx:**
- Icon color: text-neutral-800 -> dark:text-[#e5e5e5]
- Underline: bg-neutral-800 -> dark:bg-neutral-200

**AboutCard.tsx:**
- Border: border-neutral-200 -> dark:border-neutral-700
- Hover border: hover:border-neutral-300 -> dark:hover:border-neutral-600
- Text: text-neutral-600 -> dark:text-neutral-400
- Background (if any): add dark variant

**GlobalBackground.tsx:**
- Grid stroke: currentColor (inherits) - ensure parent has dark:text-neutral-100
- Or adjust strokeOpacity for dark mode visibility

**ScrollProgress.tsx:**
- Progress bar colors: ensure visible against both backgrounds
- Background track: adjust for dark mode

**ScrollToTopButton.tsx:**
- Button background: neutral colors -> dark variants
- Icon color: ensure visible
- Focus ring: adapt for dark mode

**NavItem.tsx:**
- Text colors: follow mapping
- Active state: ensure contrast
- Hover state: ensure visible

**SkillCategory.tsx:**
- Badge/tag colors: ensure readable
- Background: apply mapping
- Border: apply mapping

**CertificationItem.tsx:**
- Card background: apply mapping
- Text hierarchy: maintain in dark mode
- Border/dividers: apply mapping

**TimelineItem.tsx:**
- Timeline line: neutral-200 -> dark:neutral-700
- Date text: neutral-500 -> dark:neutral-400
- Content text: apply mapping

**KeyboardIndicator.tsx:**
- Key background: neutral-100 -> dark:neutral-800
- Key border: neutral-300 -> dark:neutral-600
- Text: ensure readable

**CopyButton.tsx:**
- Icon color: neutral-400 -> visible in both modes
- Hover state: appropriate feedback
- Success state: ensure green is visible

**SocialIcon.tsx:**
- Icon colors: neutral-500 -> dark:neutral-400
- Hover: hover:text-neutral-800 -> dark:hover:text-white
- Focus ring: adapt for dark mode

**Special handling:**
- Replace shadow-lg with glow in dark mode where applicable:
  `shadow-lg dark:shadow-[0_0_20px_0_rgb(255_255_255/0.05)]`
- Keep hover:shadow styles but add dark variants
  </action>
  <verify>
1. Run `grep -c "dark:" src/components/ui/Section.tsx` - should find dark: classes
2. Run `grep -c "dark:" src/components/ui/AboutCard.tsx` - should find dark: classes
3. Run `for f in src/components/ui/*.tsx; do echo "$f: $(grep -c 'dark:' $f 2>/dev/null || echo 0)"; done` - all should have dark: classes
4. Run `pnpm dev` and toggle theme - all UI elements should update
5. Run `pnpm build` - should complete without errors
  </verify>
  <done>
All UI components have dark mode styles, cards have proper depth, interactive elements have visible states
  </done>
</task>

<task type="auto">
  <name>Task 3: Update section components and verify complete coverage</name>
  <files>
    src/components/sections/AboutSection.tsx
    src/components/sections/ExperienceSection.tsx
    src/components/sections/EducationSection.tsx
    src/components/sections/SkillsSection.tsx
    src/components/sections/CertificationsSection.tsx
    src/components/sections/ContactSection.tsx
  </files>
  <action>
Update all section components with dark mode variants.

**AboutSection.tsx:**
- Main text: text-neutral-800 -> dark:text-[#e5e5e5]
- Description: text-lg font-light - ensure readable
- Grid layout: no background changes needed unless specified

**ExperienceSection.tsx:**
- Company names: ensure prominent
- Role titles: maintain hierarchy
- Date ranges: text-neutral-500 -> dark:text-neutral-400
- Description text: ensure readable
- Any cards/borders: apply mapping

**EducationSection.tsx:**
- Institution names: ensure prominent
- Degree/program: maintain hierarchy
- Years: apply muted text mapping
- Descriptions: ensure readable

**SkillsSection.tsx:**
- Category headers: ensure prominent
- Skill tags/badges: ensure readable with proper background
- Progress bars (if any): ensure visible

**CertificationsSection.tsx:**
- Certification names: ensure prominent
- Issuer: secondary text mapping
- Dates: muted text mapping
- Links: ensure visible hover states

**ContactSection.tsx:**
- Form inputs (if any): shadcn/ui will handle via globals.css
- Labels: ensure readable
- Buttons: ensure visible
- Links: ensure hover states

**Final verification pass:**
After updating, run through each section in dark mode to verify:
1. All text is readable (sufficient contrast)
2. All interactive elements have visible states
3. Visual hierarchy is maintained
4. No elements "disappear" against background
5. Cards have proper depth (glow effect in dark mode)

**Image handling reminder:**
If any section contains images, the global CSS from Plan 01 handles brightness reduction:
```css
.dark img { filter: brightness(0.85) contrast(1.15); }
```
No per-component changes needed for images.
  </action>
  <verify>
1. Run `for f in src/components/sections/*.tsx; do echo "$f: $(grep -c 'dark:' $f 2>/dev/null || echo 0)"; done` - all should have dark: classes
2. Run `pnpm dev` - start dev server
3. Toggle to dark mode
4. Scroll through entire page - all sections should look correct
5. Check each section specifically:
   - About: text readable, cards have borders
   - Experience: timeline visible, text hierarchy clear
   - Education: same as experience
   - Skills: badges readable, categories clear
   - Certifications: cards visible, links work
   - Contact: form (if exists) usable, links visible
6. Toggle between themes several times - smooth transitions
7. Run `pnpm build` - should complete without errors
  </verify>
  <done>
All section components styled for dark mode, complete visual coverage achieved, no elements missing dark variants
  </done>
</task>

</tasks>

<verification>
After all tasks complete, perform comprehensive visual review:

1. **Full Page Scroll Test (Light Mode):**
   - Header: readable, links work
   - Each section: proper contrast, readable
   - Footer: visible, links work
   - Toggle visible bottom-left

2. **Full Page Scroll Test (Dark Mode):**
   - Background is #0a0a0a (dark gray, not black)
   - Text is #e5e5e5 (off-white)
   - Cards have subtle glow instead of shadow
   - All interactive elements have visible hover states
   - No elements "blend" into background

3. **Interactive Elements Test (Both Themes):**
   - All links show hover state
   - All buttons have visible states
   - Copy buttons work and show feedback
   - Social icons change on hover
   - Navigation items highlight correctly

4. **Transition Test:**
   - Toggle theme multiple times
   - All elements transition together (no stagger)
   - 150ms fade (if motion not reduced)
   - Scroll position preserved

5. **Contrast Check:**
   - No text below 4.5:1 contrast ratio
   - Borders visible against backgrounds
   - Focus rings visible in both themes

6. **Production Test:**
   ```bash
   pnpm build && pnpm start
   ```
   Repeat tests in production mode.
</verification>

<success_criteria>
- All layout components (Header, Footer, Nav, MobileMenu) have dark variants
- All UI components have dark variants with proper contrast
- All section components have dark variants
- Text readable in both themes (minimum 4.5:1 contrast)
- Cards show depth (borders + glow in dark mode)
- Interactive elements have visible hover/focus states
- Shadows become subtle glows in dark mode
- Visual hierarchy maintained in both themes
- Smooth 150ms transitions between themes
- pnpm build completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-dark-mode/01-03-SUMMARY.md`
</output>
