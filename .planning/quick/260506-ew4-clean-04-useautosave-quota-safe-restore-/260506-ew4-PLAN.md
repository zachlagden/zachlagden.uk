---
status: complete
quick_id: 260506-ew4
description: CLEAN-04 — useAutoSave quota-safe + restore-on-mount
date: 2026-05-06
---

# Plan 260506-ew4

## Goal

Resolve **CLEAN-04** (CONCERNS.md #21):
- Wrap `localStorage.setItem` in try/catch and surface quota errors to the user
- Wire `load()` into BlogEditor with a "Restore unsaved draft?" prompt

## Tasks

`src/hooks/useAutoSave.ts`:
- Add `error: string | null` state
- Catch `DOMException` `QuotaExceededError`; surface a friendly message + console.warn
- Reset `error` to null on a subsequent successful save
- Return `{ save, load, clear, error }`

`src/components/admin/BlogEditor.tsx`:
- Use `useEffect` with one-shot `checkedRestoreRef` to call `loadAutoSave()` once on mount
- If a saved draft exists AND differs from current state → set `draftToRestore` state
- Render an amber banner above the form with Restore (apply) / Discard (clear) buttons
- Render a red banner above the form when `autoSaveError` is non-null

## Verification

`pnpm tsc --noEmit && pnpm lint` — both 0.

## Commit

`fix(autosave): wrap setItem in try/catch and wire restore-on-mount`
