---
status: complete
quick_id: 260506-fu2
description: POLISH-05 — image compression
date: 2026-05-06
---

# Plan 260506-fu2

## Goal

Re-compress OG/Twitter/icon PNGs. Resolves **POLISH-05** (CONCERNS.md #19).

## Approach

Use `sharp` (already installed transitively via Next) for palette quantization. Keep PNG format so existing URLs in metadata don't change.

## Tasks

- Add `sharp` to devDependencies (so a Node CLI script can resolve it)
- Write `scripts/optimize-images.mjs` — reusable for future image work
- Run it on the 5 brand PNGs

## Verification

Visual check after re-compress (palette mode is imperceptible for branding text + simple graphics).

## Commit

`perf(assets): re-compress OG/Twitter/icon PNGs with sharp palette`
