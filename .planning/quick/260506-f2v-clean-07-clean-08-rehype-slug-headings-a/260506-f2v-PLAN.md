---
status: complete
quick_id: 260506-f2v
description: CLEAN-07 + CLEAN-08 — rehype-slug headings and real sitemap dates
date: 2026-05-06
---

# Plan 260506-f2v

## Goal

Resolve **CLEAN-07** (CONCERNS.md #31) and **CLEAN-08** (CONCERNS.md #30) in one atomic commit (related — both concern blog content lifecycle).

## CLEAN-08: rehype-slug for markdown heading IDs

`src/components/blog/MarkdownRenderer.tsx`:
- Add `rehype-slug` plugin to the pipeline between `rehypeRaw` and `rehypeSanitize`
- Drop the local children-based id derivation from h1/h2/h3 components — they forward whatever id rehype-slug added. Headings with inline markdown (e.g. `## Why **GSD** matters`) now get a real ID instead of being silently un-linkable.

`src/components/blog/TableOfContents.tsx`:
- Use `github-slugger` (the same library rehype-slug uses internally) so anchors match exactly
- Strip inline markdown emphasis from the heading text used for both the visible label and the slug source
- Use `String.matchAll` for the markdown heading scan

Adds `rehype-slug@^6.0.0` and `github-slugger@^2.0.0` as runtime dependencies.

## CLEAN-07: real sitemap dates

`src/lib/blog.ts`:
- Add `getAllPublishedSlugsWithDates()` returning `{ slug, updatedAt }[]`. Coerces non-Date values to Date defensively.

`src/app/sitemap.ts`:
- Module-scope `BUILD_DATE` constant: `process.env.BUILD_DATE` if set, else `new Date()` (per-process stable timestamp)
- Static homepage and `/blog` use `BUILD_DATE`
- Blog post URLs use `post.updatedAt`

## Verification

`pnpm tsc --noEmit && pnpm lint` — both 0.

## Commit

`fix(blog): rehype-slug for headings + real sitemap dates`
