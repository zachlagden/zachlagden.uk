---
status: complete
quick_id: 260506-ee3
description: SEC-04 — auth adapter fail-loud logging
date: 2026-05-06
---

# Plan 260506-ee3: auth adapter fail-loud

## Goal

`getAdapter()` in `src/lib/auth.ts` must `console.error` the underlying error instead of silently returning `undefined` when MongoDB initialisation fails.

Resolves **SEC-04** (CONCERNS.md #17).

## Task

Single-line catch block change:

```diff
-  } catch {
+  } catch (err) {
+    console.error(
+      "[auth] MongoDB adapter failed to initialise — falling back to JWT-only sessions...",
+      err,
+    );
     return undefined;
   }
```

## Verification

- `pnpm tsc --noEmit` — exit 0
- `pnpm lint` — exit 0

## Commit

`fix(auth): log MongoDB adapter init failure instead of silent fallback`
