---
status: complete
quick_id: 260506-et0
description: PERF-04 — BlogSearch skip initial empty-query fetch
date: 2026-05-06
---

# Plan 260506-et0

## Goal

Stop the debounced search effect from firing `onSearch("")` on mount, which duplicated the SSR-shipped blog list data. Resolves **PERF-04** (CONCERNS.md #11).

## Task

`src/components/blog/BlogSearch.tsx`:
- Add `isFirstRunRef = useRef(true)`
- In the debounce effect: if first run, flip the ref to false and return early
- Subsequent renders (real user input) trigger the 300ms debounce as before

## Verification

`pnpm tsc --noEmit && pnpm lint` — both 0.

## Commit

`perf(blog): skip BlogSearch initial empty-query fetch`
