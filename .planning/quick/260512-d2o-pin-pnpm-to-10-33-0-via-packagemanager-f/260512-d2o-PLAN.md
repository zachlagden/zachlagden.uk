---
phase: quick-260512-d2o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - BUILD-FIX-01
must_haves:
  truths:
    - "Corepack resolves pnpm to exactly 10.33.0 on every host (dev, CI, Docker build)"
    - "`pnpm install --frozen-lockfile` succeeds locally without lockfile drift"
    - "Docker build no longer pulls pnpm 11 and no longer hits the `node:sqlite` Node 22 requirement"
    - "Dockerfile and pnpm-lock.yaml are unchanged"
  artifacts:
    - path: "package.json"
      provides: "Top-level `packageManager` field pinning pnpm 10.33.0 for Corepack"
      contains: '"packageManager": "pnpm@10.33.0"'
  key_links:
    - from: "package.json `packageManager` field"
      to: "Dockerfile `corepack enable pnpm` (deps + builder stages)"
      via: "Corepack reads packageManager and installs the exact version before running `pnpm install`"
      pattern: 'packageManager.*pnpm@10\.33\.0'
---

<objective>
Add a top-level `"packageManager": "pnpm@10.33.0"` field to `package.json` so Corepack pins the pnpm version that runs in the Coolify Docker build (Node 20-alpine). Without this, Corepack downloads pnpm 11.1.0, which requires Node 22.13+ for `node:sqlite` and crashes the `deps` stage.

Purpose: Unblock the failed prod deploy (uuid p10k8gnslfmghqwjibgdsa2q) and lock the toolchain version across dev/CI/prod via the canonical Corepack mechanism.

Output: One-line addition to `package.json`; lockfile and Dockerfile untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@./package.json
@./Dockerfile
@./pnpm-lock.yaml

<interfaces>
<!-- Current top-level shape of package.json (relevant keys only). The `packageManager`
     field is a sibling of `name`, `version`, `private`, `scripts`, `dependencies`,
     `devDependencies`, and `pnpm`. Insert it once; Corepack consumes it automatically. -->

```json
{
  "name": "cv.zachlagden.uk",
  "version": "1.0.0",
  "private": true,
  "scripts": { ... },
  "dependencies": { ... },
  "devDependencies": { ... },
  "pnpm": { "onlyBuiltDependencies": [...] }
}
```

Corepack contract: when `packageManager` is set to `"<tool>@<version>"`, every Corepack-shimmed invocation (`pnpm ...`) on that project will download and use exactly that version. No hash needed — Corepack fetches and caches on first use. The lockfile (`lockfileVersion: 9.0`) is already pnpm 9/10 compatible, so pinning to 10.33.0 introduces zero drift.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add packageManager field pinning pnpm 10.33.0</name>
  <files>package.json</files>
  <action>
    Edit `package.json` to add a top-level `"packageManager": "pnpm@10.33.0"` field. Place it immediately after the `"private": true` line, before `"scripts"`, so the canonical metadata stays clustered with `name`/`version`/`private`. Do NOT include a hash — bare `pnpm@10.33.0` is the documented Corepack form and is what Zach already uses locally (per `pnpm --version`). Preserve existing 2-space indentation, double quotes, and trailing newline. Do NOT touch any other field, dependency version, or the `pnpm.onlyBuiltDependencies` block. Do NOT modify `Dockerfile` or `pnpm-lock.yaml`. After the edit, run `pnpm install --frozen-lockfile` to confirm Corepack resolves to 10.33.0 cleanly and the existing `lockfileVersion: 9.0` lockfile is still valid with zero drift. If `pnpm install --frozen-lockfile` reports any lockfile modification, treat it as a failure and stop — the lockfile must not change in this fix.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.packageManager!=='pnpm@10.33.0'){process.exit(1)}" && pnpm install --frozen-lockfile && git diff --exit-code pnpm-lock.yaml Dockerfile</automated>
  </verify>
  <done>
    `package.json` contains `"packageManager": "pnpm@10.33.0"` at the top level. `pnpm install --frozen-lockfile` exits 0 with no lockfile changes. `git diff` shows changes ONLY to `package.json` (one added line). `Dockerfile` and `pnpm-lock.yaml` are byte-identical to HEAD.
  </done>
</task>

</tasks>

<verification>
- `node -p "require('./package.json').packageManager"` prints `pnpm@10.33.0`.
- `pnpm install --frozen-lockfile` succeeds with no warnings about lockfile drift.
- `git status --porcelain` shows only `M package.json` (no other tracked files modified).
- `grep -c "FROM node:20-alpine" Dockerfile` is unchanged (3 — deps, builder, runner stages).
</verification>

<success_criteria>
- Single one-line addition to `package.json` declaring `"packageManager": "pnpm@10.33.0"`.
- Local `pnpm install --frozen-lockfile` is a no-op against the existing lockfile.
- Dockerfile and pnpm-lock.yaml are untouched.
- Ready to commit with: `fix(build): pin pnpm to 10.33.0 via packageManager to unblock prod docker build`
- Next Coolify deploy will see Corepack fetch pnpm 10.33.0 (Node-20-compatible) instead of latest 11.x, resolving the `node:sqlite` crash.
</success_criteria>

<output>
After completion, create `.planning/quick/260512-d2o-pin-pnpm-to-10-33-0-via-packagemanager-f/260512-d2o-01-SUMMARY.md` recording:
- The exact line added and its position in `package.json`.
- Output of `pnpm --version` and `pnpm install --frozen-lockfile` (confirming no drift).
- Confirmation that `Dockerfile` and `pnpm-lock.yaml` were not modified.
- Note for the next deploy: re-trigger Coolify deployment after the commit lands on `main`.
</output>
